// Question 3

#include <stdio.h>

void main() {
    int a;                              // declares an integers identified by 'a'
    printf("Please enter the number");  // prints the quoted line to the console
    scanf("%d", &a);                    // takes an integer input and assigns it to 'a'
    printf("%d", a);                    // prints the value of integer 'a' to the console
}
