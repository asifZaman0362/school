// Question 4

#include <stdio.h>

void main() {
    float b;                            // declares an integer identified by 'b'
    printf("Please enter the number");  // prints the quoted line to the console
    scanf("%d", &b);                    // takes in an integer and tries to assigns it to 'b' but fails due to unexpected type
}
